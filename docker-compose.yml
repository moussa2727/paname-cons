version: '3.8'
services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: panameconsultingDb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: fXLfojVGjUGRXhkGcPjgqlJGFVBfIWAK
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    ports:
      - "10000:10000"
    depends_on:
      - postgres
    environment:
      DATABASE_URL: postgresql://postgres:iByZAAedmCwNNDKkuxjyVQuEnGswpDGR@ballast.proxy.rlwy.net:41731/panameconsultingDb

volumes:
  postgres_data: