[phases.setup]
nixPkgs = ["nodejs_18", "pnpm"]
 
[phases.build]
cmds = ["cd backend && pnpm install", "cd backend && pnpm run build", "cd backend && pnpm prisma generate"]
 
[start]
cmd = "cd backend && node dist/src/main.js"
 
[variables]
NODE_ENV="production"
PORT="10000"
HOST="0.0.0.0"