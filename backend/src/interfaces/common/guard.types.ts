// =================================
// TYPES GUARDS (Gardes d'accès)
// =================================

export interface GuardMetadata {
  roles?: string[];
  permissions?: string[];
  public?: boolean;
}
