// =================================
// TYPES DECORATORS (Décorateurs personnalisés)
// =================================

export interface DecoratorMetadata {
  roles?: string[];
  permissions?: string[];
  public?: boolean;
}
