// =================================
// TYPES DEVELOPMENT (Développement)
// =================================

export interface DevConfig {
  hotReload: boolean;
  debug: boolean;
  profiling: boolean;
  tracing: boolean;
  mockData: boolean;
  apiDocs: boolean;
  playground: boolean;
}
